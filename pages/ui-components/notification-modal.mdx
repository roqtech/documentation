import { Callout } from 'nextra-theme-docs'
import { Tab, Tabs } from 'nextra-theme-docs';
import Image from 'next/image';

# Notification Modal UI component

## Overview

The `<NotificationModal />` component shows the user's notifications in real-time and also enables the user to
configure the preferences. Please refer to the [notifications feature guide](/feature-guides/notifications) for more
information and instructions.

<Tabs items={['Light', 'Dark']}><Tab>

<Image src={"/notification-modal-light.png"} alt="NotificationModal light" width={600} height={160} />

</Tab><Tab>

<Image src={"/notification-modal-dark.png"} alt="NotificationModal dark" width={600} height={160} />

</Tab></Tabs>

See this component in action on
[Storybook](https://ui.roq.tech/?path=/story/notifications-notificationsmodal--default).

## Integration

{/* TODO Props are not aligned with the actual UI component. */}

The notification modal component can be integrated with just one line of code. The component will open a socket
connection to ROQ Platform to receive notifications events in real time. The list of notifcations is fetched using the
[notifications() API](/api-doc/notifications-api#notifications).

<Tabs items={['React']}><Tab>

```tsx
<NotificationModal/>
```

</Tab></Tabs>