# Postgres

ROQ BaaS using [Postgres](https://www.postgresql.org/) as the application database. It's read only for now, so you can't write to it.

## Database connection string

The Postgres connection string can be found in the [ROQ Console](https://console.roq.tech/). 