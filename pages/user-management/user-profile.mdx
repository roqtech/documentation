import { Callout } from "nextra-theme-docs";
import { Tab, Tabs } from "nextra-theme-docs";
import Image from "next/image";
import DoBasicSetup from "/snippets/do-basic-setup.mdx";

# User Profile Component

## Introduction

The `<UserProfile/>` component can be used to show the user's profile information, along with the ability to edit the profile.
It includes an avatar uploader, and a form to edit the user's password, and other profile information.

<Tabs items={['Light', 'Dark']}><Tab>

<Image src={"/user-profile.png"} alt="user-profile" width={500} height={595} />

</Tab><Tab>

<Image
  src={"/user-profile-dark.png"}
  alt="user-profile-dark"
  width={500}
  height={595}
/>

</Tab></Tabs>

## Integration

<DoBasicSetup />

### `<UserProfile/>`

To show the UserProfile, you can add this snippet to your page:

<Tabs items={['Reactjs', 'Next.js']}><Tab>

```tsx
import { UserProfile } from "@roq/ui-react";

function App() {
  return <UserProfile />;
}
```

</Tab>
<Tab>

```tsx
import { UserProfile } from "@roq/nextjs";

function App() {
  return <UserProfile />;
}
```

</Tab>
</Tabs>
