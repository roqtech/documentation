# Add Authentication

Common feature for any web application is user authentication. The ROQ BaaS provides easy user authentication through it's SDK and for Next.js project you can use `signIn`, `signOut`, and `signUp` method from the `useRoqClient()` API. 

## Installation


## Adding login, logout and signup buttons

## Handling the session 

{/*
(explain the token, access to the session data)
*/}