import { Callout, Tab, Tabs} from "nextra-theme-docs"

# Filtering Data by User Roles

User roles will determine what kind of access each user has to data in a system. For example, different user with different roles will have different data access to `/video` routes