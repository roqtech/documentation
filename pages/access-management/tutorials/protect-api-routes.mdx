import { Callout, Tab, Tabs} from "nextra-theme-docs"

# How to Protect API Routes

To protect API routes in a ROQ-generated application, it is essential to verify the user's role access and permissions.

In the generated application this protection is done by the `hasAccess()` method.



