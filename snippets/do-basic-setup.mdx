import { Callout } from "nextra-theme-docs";
import { Tab, Tabs } from "nextra-theme-docs";
import Image from "next/image";


<Callout type="info" emoji="ℹ️">
  Before you can use this UI Component, please do the [basic setup](/installation-guides/basic-setup). 
</Callout>

  while generating the page. Any console logs will be displayed in the terminal window. while generating the page. Any console logs will be displayed in the terminal window.